# Fix Requirements & Issue Tracking

This document tracks bug fixes, errors, and requirement adjustments during development and operation.

## Log
* (2026-08-04) Initialized project tracking. Ghi nhận URL tạo ảnh và kịch bản chờ 15s cho logo.
* (2026-08-04) Cập nhật luồng lấy ảnh: Sử dụng ảnh có sẵn trong Uploads thay vì paste ảnh. Đổi sang tải ảnh bằng nút Download thay vì trích xuất Base64 trực tiếp.
* (2026-08-04) **Lỗi ChromeDriver**: Gặp lỗi `[Errno 8] Exec format error` trỏ vào file `THIRD_PARTY_NOTICES.chromedriver`. Đây là bug của `webdriver-manager` trả về sai đường dẫn file thực thi.
* (2026-08-04) **Lỗi Selenium Manager**: Khi bỏ `webdriver-manager` để dùng Selenium Manager mặc định thì gặp lỗi `Unable to obtain driver for chrome`. Nguyên nhân do hệ thống không tìm được đúng phiên bản Chrome/Chromedriver tương thích.
* (2026-08-04) **Giải pháp Driver**: Quay lại dùng `webdriver-manager` nhưng áp dụng mẹo (workaround): Nếu đường dẫn trả về chứa chữ `THIRD_PARTY_NOTICES`, code sẽ tự động replace chuỗi đó thành `chromedriver` để trỏ đúng vào file thực thi thực sự.
* (2026-08-04) **Lỗi Permission Denied**: Gặp lỗi `executable may have wrong permissions`. Đây là do file `chromedriver` tải về không có quyền thực thi (executable). Giải pháp: Dùng `os.chmod(driver_path, 0o755)` để cấp quyền thực thi tự động trước khi khởi chạy WebDriver.
* (2026-08-04) **Lỗi Không tìm thấy Element do chưa Đăng nhập**: Quá trình Automation thất bại do bị văng ra màn hình đăng nhập tài khoản Google. Google chặn đăng nhập bằng Automation. Giải pháp: Thêm cấu hình `--user-data-dir` vào Selenium để lưu profile và viết file `login.py` để người dùng tự đăng nhập tay 1 lần, giúp vượt qua rào cản bảo mật của Google.
* (2026-08-04) **Lỗi JS Selector không hoạt động**: Thuật toán tìm Element dựa trên JS (DOM Position, bounding rects) bị thất bại âm thầm. 
  - Nút `+`, `Uploads`, `Add to prompt`, Nhập prompt bị trượt hoàn toàn.
  - Nút `Download` trên modal không được tìm thấy.
  - Chỉ duy nhất phần click vào ảnh để phóng to là hoạt động.
  - Giải pháp dự kiến: Yêu cầu người dùng lấy trực tiếp XPath hoặc CSS Selector chính xác từ trình duyệt (Inspect Element) hoặc dùng thư viện giả lập phím/chuột.
* (2026-08-04) **Lỗi Cú pháp (Syntax Error)**: Quá trình click Add to Prompt thành công, nhưng bị lỗi `name 'webdriver' is not defined` khi thực thi lệnh bấm phím ESC (Escape) đóng popup. Nguyên nhân do quên `import ActionChains`. Đã bổ sung import `ActionChains` từ `selenium.webdriver.common.action_chains` và sửa thành `ActionChains(driver).send_keys(Keys.ESCAPE).perform()`.
* (2026-08-04) **Lỗi Gửi thiếu Prompt và Lỗi "Đã có lỗi xảy ra"**: 
  - Nguyên nhân 1: Trong nội dung Prompt có chứa ký tự xuống dòng (`\n`). Khi Selenium gõ phím `\n`, ô chat của Google Flow hiểu đó là lệnh Enter (Gửi) nên nó tự động gửi luôn khi chưa gõ xong.
  - Nguyên nhân 2: Gửi ngay lập tức khi ảnh logo chưa load xong (theo kịch bản cần đợi 15 giây).
  - Giải pháp ban đầu: Chuyển toàn bộ dấu `\n` thành tổ hợp phím `Shift + Enter` và thêm lệnh đợi 15 giây.
* (2026-08-04) **Lỗi Gửi thành 3 Prompt (Shift+Enter không hoạt động)**: Giải pháp `Shift + Enter` bị thất bại trên Slate.js của Google, phím này vẫn bị trình duyệt/React bắt dưới dạng lệnh `Enter` khiến 1 đoạn prompt dài bị cắt vụn và gửi thành 3 tin nhắn rời rạc, dẫn đến sinh lỗi "Something went wrong".
  - Giải pháp tối ưu: Bỏ việc gõ phím từng chữ. Sử dụng JavaScript để tạo một sự kiện giả lập dán nội dung (`ClipboardEvent('paste')`). Việc này sẽ khiến Slate.js nhận diện toàn bộ văn bản (bao gồm cả dấu xuống dòng) như thể người dùng vừa bấm `Ctrl+V`, giúp nạp toàn bộ prompt vào 1 tin nhắn duy nhất mà không kích hoạt lệnh Gửi.
* (2026-08-04) **Lỗi Paste Event bị chặn**: Javascript `ClipboardEvent` bị Slate.js/React chặn hoàn toàn (Bảo mật trình duyệt không cho script tự can thiệp clipboard). Hậu quả là khung prompt trống trơn, dẫn đến gửi mỗi logo và bị lỗi "Something went wrong".
  - Cập nhật thời gian chờ: Giảm thời gian chờ load ảnh từ 15s xuống 2s theo yêu cầu.
  - Giải pháp Tối giản & Hiệu quả nhất: AI sinh ảnh không quan tâm đến dấu xuống dòng. Thay vì cố gắng nhập dấu xuống dòng, ta dùng `prompt_text.replace('\n', ' ')` để biến toàn bộ thành 1 đoạn văn duy nhất, sau đó dùng lệnh `send_keys` bình thường. Việc này giải quyết 100% lỗi gửi nhầm do phím Enter.
* (2026-08-04) **Lỗi Gửi non do Ký tự ngầm `\r`**: Hàm `replace('\n', ' ')` đã bỏ sót ký tự Carriage Return (`\r`) sinh ra từ các hệ điều hành như Windows (`\r\n`). Khi hàm `send_keys` gặp ký tự `\r` này, nó vẫn biên dịch thành lệnh gõ phím Enter. Dẫn tới text chưa kịp paste hết đã gửi. Giải pháp: Xóa sạch cả `\r` lẫn `\n`. Thay phương thức `send_keys` bằng Clipboard giả lập tổ hợp phím `Ctrl + V`.
* (2026-08-04) **Lỗi Không giữ được Phiên đăng nhập (Session)**: Khi chuyển sang `undetected-chromedriver`, việc truyền đối số `--user-data-dir` bị thư viện này xử lý khác biệt, dẫn đến việc luôn mở một trình duyệt trắng (chưa đăng nhập Google) hoặc không đọc được profile cũ. 
* (2026-08-04) **Bí ẩn "Something went wrong" Đã được Giải Mã Hoàn Toàn**: 
  - Trước đó tôi có suy luận rằng "Selenium click đè làm mất Logo" và "Thiếu Logo nên Google báo lỗi". Tuy nhiên, **suy luận này hoàn toàn sai lầm**! Theo xác nhận từ thực tế của người dùng: (1) Prompt gửi đi **đã có chứa sẵn ảnh logo** bên trong. (2) Kể cả khi có cố tình gửi prompt mà không đính kèm logo đi chăng nữa, Google Flow **vẫn trả về ảnh bình thường** (chỉ là sai format) chứ không hề văng lỗi "Something went wrong".
  - **Kết luận cuối cùng**: Việc payload hoàn toàn hợp lệ (đầy đủ chữ và ảnh) nhưng vẫn bị Google Backend chặn đứng bằng lỗi "Something went wrong" **chứng minh 100% đây là Hệ thống Bot Detection của Google**. Nó đã đánh hơi thấy dấu vết của thư viện Selenium gốc và cấm cửa session đó thực hiện gọi API sinh ảnh.
  - **Hướng đi tiếp theo bắt buộc**: Chúng ta không thể trốn tránh việc phải dùng `undetected-chromedriver`. Bắt buộc phải đưa hệ thống sang UC Chrome, đồng thời cấu hình cẩn thận `user_data_dir` để giữ được Session đăng nhập, thì mới có thể qua mặt được con mắt tinh tường của Google.
* (2026-08-04) **Lỗi Bot Detection do Hành vi Gõ Phím (Behavioral Analysis)**: Dù đã dùng `undetected-chromedriver` và gửi thành công bằng phím Enter (thay vì click), hệ thống vẫn tiếp tục báo lỗi "Something went wrong". Ban đầu tôi nghĩ do tốc độ paste quá nhanh nên đã chuyển sang cơ chế Human-like Typing. Tuy nhiên, người dùng đã thử nghiệm và xác nhận: **Khi thao tác bằng tay (Copy - Paste) thì KHÔNG bị lỗi**. 
  - Điều này chứng tỏ lỗi **KHÔNG PHẢI do paste nhanh**, mà là do **cách Selenium gửi sự kiện bàn phím**. Khi dùng `send_keys` (dù là gõ từng chữ hay ấn Ctrl+V), Selenium tạo ra các sự kiện bàn phím "tổng hợp" (Synthetic Events) không có thuộc tính `isTrusted=true` của hệ điều hành. Slate.js của Google nhận diện được đây là tín hiệu giả lập của Bot nên từ chối xử lý lệnh hoặc sinh ra payload rỗng.
  - Giải pháp: Sử dụng thư viện `pyperclip` để copy text vào Clipboard của HĐH Ubuntu, và dùng thư viện `pynput` để giả lập phím tắt `Shift + Insert` dán text trực tiếp từ HĐH. 
* (2026-08-04) **Lỗi Pyperclip Not Implemented Error trên Linux**: Khi chạy trên Linux/Ubuntu, `pyperclip` văng lỗi không tìm thấy cơ chế Clipboard (do thiếu các gói tiện ích hệ thống như `xclip` hoặc `xsel`). Giải pháp: Bắt buộc người dùng phải cài đặt gói phần mềm xclip thông qua lệnh `sudo apt-get install xclip`.
* (2026-08-04) **Lỗi Pynput gõ phím vào hư vô (Focus Mismatch)**: Việc dùng `pynput` gửi tín hiệu bàn phím vật lý (`Shift+Insert`) tuy an toàn nhưng lại dính chí mạng ở một điểm: Trình duyệt bị mất Focus ở tầng OS (do terminal đang chiếm Focus khi chạy script). Hành động `window.focus()` của Javascript bị hệ điều hành chặn lại để chống cướp focus (Focus stealing prevention). Kết quả là phím `Shift+Insert` và `Enter` bị gõ vào terminal hoặc hư vô, prompt không được dán vào khung chat. Giải pháp: Hủy bỏ pynput. Quay trở lại tận dụng `pyperclip` kết hợp với Selenium native `send_keys(Keys.CONTROL, 'v')` để dán chính xác vào Element, ĐỒNG THỜI truyền đoạn prompt **nguyên bản đa dòng (multiline)** thay vì đoạn prompt đã bị xóa `\n` (bởi có khả năng Google chặn việc format sai cú pháp prompt).
* (2026-08-04) **Lỗi CDP Ctrl+V không đọc được Clipboard**: Trong quá trình nỗ lực dùng Chrome DevTools Protocol (CDP) để gửi phím cứng `Ctrl+V`, mặc dù tín hiệu bàn phím gửi đi rất chuẩn, nhưng đoạn text VẪN KHÔNG ĐƯỢC PASTE. Nguyên nhân: Giao thức CDP giao tiếp trực tiếp với DOM engine của trình duyệt, bỏ qua hệ quản lý cửa sổ (Window Manager) của Hệ điều hành. Do đó, tổ hợp phím ảo `Ctrl+V` của CDP không hề móc nối được với OS Clipboard (bộ nhớ đệm hệ điều hành) để lấy chữ ra dán. Giải pháp: Phải kết hợp giữa `send_keys(Keys.CONTROL, 'v')` của Selenium (để ép trình duyệt kích hoạt lệnh paste từ Clipboard) và `CDP Enter` (để gửi đi an toàn mà không bị phát hiện là Bot click).
* (2026-08-04) **Lỗi CDP Input.insertText không thể inject text vào Slate.js**: Khi chuyển sang dùng CDP `Input.insertText` (kể cả line-by-line), khung chat vẫn trống trơn không nhận chữ. Nguyên nhân: `Input.insertText` yêu cầu con trỏ (cursor) phải được focus chính xác vào Text Node bên trong DOM. Các thao tác CDP Mouse Click đã focus vào Container của Slate.js nhưng không đặt được con trỏ vào đúng Text Node. Việc thiếu con trỏ khiến Text bị reject hoàn toàn. Khung chat rỗng dẫn đến việc click Send thất bại hoặc bị báo 'Something went wrong'.
* (2026-08-04) **Lỗi giả lập Typing CDP quá chậm và bị bắt bài**: Để giải quyết lỗi Focus, tôi đã ngắm click vào Tâm (Center) của ô chat và dùng vòng lặp gửi tín hiệu CDP `char` từng ký tự. Mặc dù chữ ĐÃ ĐƯỢC NHẬP VÀO, nhưng tốc độ gõ quá chậm (dù đã set delay 0.005s) và Google vẫn văng lỗi "Something went wrong". Nguyên nhân: Hành vi gõ phím đều đặn như máy khâu hoặc các tín hiệu `Shift+Enter` ảo đã bị phát hiện. Giải pháp: Quay lại dùng `Input.insertText` bắn nguyên từng dòng (cực nhanh) vì bây giờ tọa độ Click Tâm (Center) đã lấy được Focus thành công!
* (2026-08-05) **Lỗi Bot Detection do cạn kiệt Điểm Tin Cậy (Trust Score / IP Flagged)**: Sau khi thử nghiệm cả phương án gõ chữ từng ký tự bằng CDP (Human Typing) và di chuyển chuột theo đường cong (ActionChains Natural Clicks), con Bot vẫn bị từ chối bằng lỗi "Something went wrong", kể cả khi đổi sang một Prompt cực ngắn ("Quả táo"). Thử nghiệm thủ công trên cùng trình duyệt đó (Copy Paste bằng tay, Bấm bằng tay) VẪN LỖI.
  - **Kết luận**: Google không bắt lỗi ở thao tác gõ chữ hay thao tác click (vì đã làm hoàn hảo). Google đã **ghi vào danh sách đen (Blacklist) địa chỉ IP** của máy chủ sau quá nhiều lần gọi API sinh ảnh liên tục bằng Bot trong 1 ngày, hoặc đánh dấu chính session của `undetected_chromedriver` đó là độc hại từ lúc khởi động. 
  - **Giải pháp triệt để**: Yêu cầu người dùng **đổi IP (Reset Router, dùng 4G, VPN)**. Sau khi IP được làm mới và Trust Score được khôi phục, toàn bộ luồng cơ chế gõ phím Human Typing CDP + Natural Mouse Click đã vượt qua bài test dễ dàng và sinh ảnh thành công! Cơ chế thao tác giả lập người thật của chúng ta là hoàn toàn chính xác, chỉ là cần một IP sạch để phát huy tác dụng.
* (2026-08-06) **Cấu hình Proxy và Vượt qua Bot Detection (Giai đoạn 2)**:
  - Bổ sung hệ thống luân chuyển IP thông qua cấu hình `proxy_config.txt` để qua mặt Bot Detection khi IP máy chủ (Fedora) bị block.
  - Tích hợp **Chrome Proxy Auth Extension (Unpacked)**: Chế tạo Extension động (Unpacked Directory) cho phép truyền Username/Password/Geo-Targeting vào Proxy-Cheap mà không bị `undetected_chromedriver` từ chối (giải quyết lỗi bypass proxy do file `.zip` không tương thích).
  - **Tắt rò rỉ WebRTC**: Vô hiệu hóa `webrtc.ip_handling_policy` để tránh rò rỉ IP thật ra ngoài khi dùng Proxy.
  - Tái tạo session tự động: Bổ sung logic khởi động lại toàn bộ trình duyệt (`driver.quit()`) sau mỗi 30 bức ảnh để làm mới hoàn toàn dấu vân tay trình duyệt (Fingerprint) và ép Proxy-Cheap xoay IP mới.
* (2026-08-06) **Cập nhật FastAPI**:
  - Chuyển đổi `@app.on_event("startup")` sang `lifespan` context manager để tương thích chuẩn mới, loại bỏ cảnh báo DeprecationWarning.
  - Thêm một endpoint WebSocket giả (Dummy) tại `/notifications/hub` chặn lỗi `403/404` từ các phần mềm gọi API bên thứ ba.
* (2026-08-07) **Lỗi Bot Detection do Dùng Trình Duyệt Ngầm (`headless=new`)**: Google vẫn phát hiện Bot kể cả khi dùng `--headless=new` và `--enable-gpu`.
  - Giải pháp: Cài đặt môi trường XFCE Desktop vật lý trên Fedora Server. Đổi code thành `headless=False` để ép Chrome hiển thị UI lên màn hình vật lý, dùng GPU vật lý 100% giống người dùng thật.
* (2026-08-07) **Lỗi SessionNotCreatedException qua SSH (X11 Permission)**: Khi gọi `DISPLAY=:0 python3 main.py` từ phiên SSH root, Chrome bị crash vì bảo mật X11 không cho root vẽ lên màn hình của user.
  - Giải pháp 1: Mở Terminal trực tiếp trên Desktop XFCE gõ `xhost +` để mở khóa.
  - Giải pháp 2: SSH bằng đúng tài khoản đang login vào XFCE.
* (2026-08-07) **Lỗi Zombie Process Chromium gây kẹt Port**: Hàm `force_kill_chrome` cũ chỉ `pkill chrome`, bỏ sót tiến trình `chromium-browser` trên Fedora, gây lỗi không tạo được Session. Đã cập nhật hàm để kill cả `chromium`.
* (2026-08-07) **Tối ưu Anti-bot: Loại bỏ F5 liên tục**: Việc `driver.refresh()` sau mỗi ảnh là hành vi phi logic. Đã sửa lại code: Nếu đang ở đúng URL thì tiếp tục gõ prompt luôn không cần tải lại trang.
* (2026-08-07) **Lỗi Timeout 100s của Cloudflare Tunnel**: Hàng đợi (Queue) sinh ảnh mất hơn 150s (gồm chờ đợi và ngủ 60s/ảnh chống bot). Backend gọi API bị Cloudflare tự động cắt kết nối (524 Timeout) vì vượt ngưỡng 100s không có dữ liệu.
  - Giải pháp: Chuyển đổi endpoint `/generate` thành **StreamingResponse (Chunked Transfer)**. API lập tức trả HTTP 200, sau đó lén bơm 1 byte khoảng trắng (` `) mỗi 15s để giữ mạng (Keep-alive), cuối cùng mới trả JSON Base64. (Dựa trên tiêu chuẩn RFC 8259, JSON parser ở client tự lờ đi khoảng trắng thừa).
